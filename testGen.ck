12 => int sections;

for( 0 => int i; i < 12; i++ ) {
  // part b
  // at least 1 on (1), stereo, at least 1 pulsing (2), shotgun



  // part c
  // at least 1 on (1), shotgun, at least 1 pulsing(2), shotgun
}
